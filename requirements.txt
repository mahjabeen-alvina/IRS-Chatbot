# --- Core app dependencies ---
streamlit==1.50.0
python-dotenv
google-generativeai

# --- NLP / Embeddings ---
sentence-transformers
faiss-cpu

# --- Utilities ---
PyMuPDF
numpy<2
pandas
protobuf
tqdm
typing-extensions
```

This removes all version pins and lets pip resolve compatible versions automatically.

## To Debug Further:

Can you click on **"Manage app"** in Streamlit Cloud and scroll through the logs to find the **actual error message**? It should show something like:
```
ERROR: Could not find a version that satisfies the requirement torch==2.0.1
```

or
```
ERROR: Failed building wheel for [package-name]

